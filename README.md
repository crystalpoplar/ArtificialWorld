# ArtificialWorld

Small collection of utility modules. Install locally for development with:

pip install -e .

To build distribution files:

python -m pip install --upgrade build
python -m build

Then install the wheel from dist/, for example:

python -m pip install dist/artificialworld-0.1.0-py3-none-any.whl
